appId: com.metis.app
productName: metis
asar: true

electronFuses:
  runAsNode: false
  enableCookieEncryption: true
  enableNodeOptionsEnvironmentVariable: false
  enableNodeCliInspectArguments: false
  enableEmbeddedAsarIntegrityValidation: true
  onlyLoadAppFromAsar: true
  grantFileProtocolExtraPrivileges: true

directories:
  output: release/bundle
  buildResources: build

files:
  - 'dist{,/**/*}'
  - 'dist-electron{,/**/*}'
  - 'node_modules{,/**/*}'
  - 'package.json'

extraResources:
  - from: sample dataset.csv
    to: sample-data/sample dataset.csv
  - from: r-api
    to: r-api
    filter:
      - .Rprofile
      - plumber.R
      - renv-bootstrap.R
      - renv.lock
      - renv/**

win:
  target: nsis
  icon: build/icon.ico
  artifactName: metis ${version} Bundle.${ext}
  extraResources:
    - from: r-api
      to: r-api
      filter:
        - R-Portable.zip

mac:
  target:
    - dmg
    - zip
  category: public.app-category.education
  minimumSystemVersion: '13.3.0'
  icon: build/icon.icns
  artifactName: metis ${version} Bundle macOS ${arch}.${ext}
  extraResources:
    - from: r-api
      to: r-api
      filter:
        - R-macos-${arch}.tar.gz

nsis:
  oneClick: true
  allowToChangeInstallationDirectory: false
  differentialPackage: false
  createDesktopShortcut: always
  installerIcon: build/icon.ico
  uninstallerIcon: build/icon.ico
  installerHeaderIcon: build/icon.ico

fileAssociations:
  - ext: metisws
    name: metis Workspace
    role: Editor
